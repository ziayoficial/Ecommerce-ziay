// ZIAY — Barrel re-export.
//
// `MarketplaceView` was split into a directory in AUDIT-FINAL-SPLIT-001
// (marketplace/{index,marketplace-listings,marketplace-my,marketplace-referrals,
// marketplace-shared}.tsx). This file preserves the original import path
// (`@/components/dashboard/marketplace-view`) so `app/page.tsx` doesn't need
// to change.

export { MarketplaceView } from './marketplace'
