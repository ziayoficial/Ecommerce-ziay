{
  "name": "ZIAY — Agente Alertas operativas de guías",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "agent-guide_alert",
        "responseMode": "responseNode"
      },
      "id": "webhook-guide_alert",
      "name": "Webhook Entrada",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://app:3000/api/agents/guide_alert",
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            {
              "name": "tenantId",
              "value": "={{ $json.body.tenantId }}"
            },
            {
              "name": "conversationId",
              "value": "={{ $json.body.conversationId }}"
            },
            {
              "name": "customerId",
              "value": "={{ $json.body.customerId }}"
            },
            {
              "name": "perfil",
              "value": "={{ $json.body.perfil }}"
            },
            {
              "name": "items",
              "value": "={{ $json.body.items }}"
            },
            {
              "name": "query",
              "value": "={{ $json.body.query }}"
            },
            {
              "name": "message",
              "value": "={{ $json.body.message }}"
            },
            {
              "name": "partialAddress",
              "value": "={{ $json.body.partialAddress }}"
            },
            {
              "name": "imageUrl",
              "value": "={{ $json.body.imageUrl }}"
            }
          ]
        }
      },
      "id": "call-agent-guide_alert",
      "name": "Llamar Agente guide_alert",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [
        220,
        0
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json }}"
      },
      "id": "respond-guide_alert",
      "name": "Responder",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        440,
        0
      ]
    }
  ],
  "connections": {
    "Webhook Entrada": {
      "main": [
        [
          {
            "node": "Llamar Agente guide_alert",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Llamar Agente guide_alert": {
      "main": [
        [
          {
            "node": "Responder",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "_notes": [
    "ZIAY Agente guide_alert — Alertas operativas de guías",
    "Endpoint: POST http://app:3000/api/agents/guide_alert",
    "Agregado en BUILD-AGENTS-LIB-001 (16 agentes adicionales sobre los 10 originales = 26 total).",
    "Importar: n8n → Workflows → Import from File → seleccionar este JSON.",
    "Activar workflow después de importar."
  ]
}
