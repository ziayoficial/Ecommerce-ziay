// ZIAY — Barrel re-export.
//
// `LogisticsIntelligenceView` was split into a directory in
// AUDIT-FINAL-SPLIT-001 (logistics/{index,logistics-scores,logistics-guides,
// logistics-alerts,logistics-shared}.tsx). This file preserves the original
// import path (`@/components/dashboard/logistics-intelligence-view`) so
// `app/page.tsx` doesn't need to change.

export { LogisticsIntelligenceView } from './logistics'
